{% extends 'base.html.twig' %}

{% block title %} la page produit{% endblock %}

{% block body %}
 

<!--<h1>Ventes Exclusives:</h1>

{% for produit in produitsPagines %}
<div>
	<img src="{{ asset('imgs/' ~ produit.image) }}" alt="{{ produit.nomProduit }}" class="card-img-top"  style="max-width: 200px;">
</div>
<div>
	<div class="col-md-8">
		<h2>{{ produit.nomProduit }}</h2>
		<h2>{{produit.prix}} € </h2>
		<p><strong>Nom du domaine :</strong> {{ produit.domaine }}</p>
		 
</div>
<div class="mt-4">
	<h4>Détails du domaine</h4>
	<p>{{ produit.domaine }}</p>
</div>
	
{% endfor %}
<div class="navigation">
  {{ knp_pagination_render(produitsPagines) }}
</div>-->
 

 

 
<h1>Ventes Exclusives:</h1>

<!--<div class="product-container ">
    {% for produit in produitsPagines %}
    <div class="col-md-4 mb-4">
        <div class="product-card">
            <img src="{{ asset('imgs/' ~ produit.image) }}" alt="{{ produit.nomProduit }}" class="card-img-top" style="max-width: 200px; margin: 0 auto;">
            <div class="card-body">
                <p class="product-name">{{ produit.nomProduit }}</p>
                <p class="product-price">{{ produit.prix }} €</p>
                <p><strong>Nom du domaine :</strong> {{ produit.domaine }}</p>
            </div>
            <div class="product-domaie">
                <h6>Détails du domaine</h6>
                <p>{{ produit.domaine }}</p>
            </div>
        </div>
    </div>
    {% endfor %}
</div>

<div class="navigation">
    {{ knp_pagination_render(produitsPagines) }}
</div>-->

 
<div class="product-container">
    {% for produit in produitsPagines %}
     <div class="product-card">
        <div class="product-img">
            <img src="{{ asset('imgs/' ~ produit.image) }}" alt="{{ produit.nomProduit }}" class="product-img"  ">

        </div>
        <h2 class="product-title">{{ produit.nomProduit }}</h2>
        <h4 class="product-price">{{ produit.prix }}  €</h4>
        <div class="product-domaine">
            <h6>Détails du domaine</h6>
            <p>{{ produit.domaine }}</p>
        </div>
        <button class="btn-ajouter-panier" data-produit-id="{{ produit.id }}"> Ajouter au Panier</button>
    </div>

    {% endfor %}
</div>
<section class="pagination">
    <div class="navigation">
    {{ knp_pagination_render(produitsPagines) }}
</div>
</section>
 


  


{% endblock %} 