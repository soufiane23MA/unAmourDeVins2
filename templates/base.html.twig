<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}unamourdevin{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️

        </text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@400..700&family=Poppins:ital,wght@0,100;0,300;0,400;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" 
        rel="stylesheet">
        {% block stylesheets %}
         <link rel="stylesheet" href="{{ asset('css/style.css') }}">
         <link rel="stylesheet" href="{{ asset('css/navbar.css') }}">
         <link rel="stylesheet" href=" {{asset('css/product.css')}}"> 
         <link rel="stylesheet" href=" {{asset('css/variables.css')}}"> 
         <!-- le lien Swiper  pour le slide page d'accueil CSS -->
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css"/>
        <link href="https://cdn.lineicons.com/5.0/lineicons.css" rel="stylesheet" />
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"> 
         
         <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" 
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
        
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/6.4.2/mdb.min.css">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/noUiSlider/15.7.0/nouislider.min.css" /> 
       
        <link rel="stylesheet" href="{{asset('css/panier.css')}}">
       <link rel="stylesheet" href=" {{asset('css/facture.css')}}"> 
       
       
        {% endblock %}
    </head>

    <body>
        <!-- Contenu normal (masqué initialement) -->
           <!-- <div id="content" style="display:none;">
               
            </div>

              Modal  
            <div id="ageCheck">
                <div class="modal">
                    <h2>Êtes-vous majeur ?</h2>
                    <p>Pour accéder à notre site, vous devez avoir l'âge légal pour consommer de l'alcool.</p>
                    <div class="buttons">
                        <button id="yes">Oui, j'ai +18 ans</button>
                        <button id="no">Non</button>
                    </div>
                </div>
            </div>-->
      <!---- la fin de la modal------>
        <nav class="navbar">
            
            <!-- Menu Burger -->
            <button class="burger-btn">☰</button>
            
            <!---------debut  de la partie centre de la navbar---->
            <div class="nav-center">
              <!-- Ton logo ou le nom de ton site -->
              <a href=" #"><img src="{{ asset('imgs/logo.png') }}" alt="Mon Logo"></a>
            </div>
            <div class="nav-right">
                <ul class="ul-right">
                    <li><a href="{{ path('app_accord_mets_vins') }}" class="nav-link">Accord Mets-Vins</a></li>
                    <li><a href="{{path('app_home')}}">Accueil</a></li>
                    <li><a href="{{path('app_home')}}">Idee De Cadeux &times;</a></li>
                    <li><a href="{{path('app_produit')}}">Produits</a></li>
                    {% if app.user %}
                    <li>
                        <a href="{{ path('app_logout') }}">Déconnexion</a>
                    </li>
                    <li>
                        <a href="{{path('app_user_profile')}}">Profile</a>
                    </li>
                {% else %}
                    <li><a href="{{ path('app_login') }}">Connexion</a></li>
                {% endif %}
                {% if is_granted('ROLE_ADMIN') %}
                <a href="{{path('admin_admin_dashbord')}}" class="text-muted small ms-3">
                    Dashbord
                </a>
                {% endif %}
                    <li><a href="{{path('app_panier')}}"><i class="fas fa-shopping-cart"></i></a></li>
                </ul>   
            </div>
        </nav>
        {% for label, messages in app.flashes %}
                {% for message in messages %}
                    <div class="alert alert-{{ label }}">
                        {{ message }}
                    </div>
                {% endfor %}
         {% endfor %}

        <!-------------------sidebar--------------- -->
        <aside class="sidebar">
            {% block sidebar %}{% endblock %}
        </aside>
        <!----------------fin block sidbar-->
         <!-- Section pour afficher les produits -->
        <div id="produitsList" >
            <!-- Les produits seront ajoutés ici dynamiquement avec JS -->
        </div>
        <!-- Inclus le formulaire de recherche par produit -->
        {{ render_search_product_form() }}

        {% block body %}
        {% endblock %}
        <footer class="foot-container">
            <div class=" ">
                <ul class=" ">
                    <li>Qui sommes-nous</li>
                    <li>Mentions légales</li>
                    <li>Suivez notre actualité</li>
                    <li>Nous contacter</li>
                </ul>
           </div>
           <div class="contact">
                <p>adresse Postale: </p>
                <p> adresse maile : </p>
                <p>Numero de telephone: </p>
                <a href="https://www.facebook.com/unamourdevin/" target="_blank" class="text-white">
                    <i class="fab fa-facebook fa-lg"></i>
                </a>
                <a href="https://instagrame.com/amourdevin_caviste" target="_blank" class="text-white">
                    <i class="fab fa-instagram fa-lg"></i>
                </a>
            </div>
           </div>
        </footer>
        
        
            <script src="{{ asset('js/script.js') }}" defer></script>
            <script src="{{ asset('js/accord.js') }}" defer></script>>
            <script src="{{ asset('js/script_commande.js') }}" defer></script>
            <script src="{{ asset('js/navbar.js') }}" defer></script>
            <script src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js"></script>
        
            <!--<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script> 
            <script src="https://cdnjs.cloudflare.com/ajax/libs/noUiSlider/15.7.0/nouislider.min.js"></script>
            <script src="https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/6.4.2/mdb.min.js"></script>  --> 
                    <!-- Modale de vérification de l'âge -->
        
         

    </body>
</html>
